# ===========================================
# Digital Library – env (copy to .env, never commit .env)
# ===========================================
# PRODUCTION: use HTTPS URLs. LOCAL: use localhost only for dev.
# ===========================================

NODE_ENV=production
PORT=3001

# Required in production: strong random secret (e.g. openssl rand -base64 32)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# MongoDB – PRODUCTION: Atlas URI (mongodb+srv://...). LOCAL: mongodb://127.0.0.1:27017/digital-library
MONGODB_URI=mongodb://127.0.0.1:27017/digital-library

# Set to true to skip seeding (use existing/real data only). Use in Docker/K8s when DB already has data.
# SKIP_SEED=true

# Frontend origin – PRODUCTION: https://your-app.vercel.app (no trailing slash). LOCAL: http://localhost:5173
CORS_ORIGIN=https://your-app.vercel.app

# -------------------------------------------
# SMTP – for forgot-password / OTP emails
# -------------------------------------------

FROM_NAME=Digital Library
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=jamalsadaf3@gmail.com
SMTP_PASS=bdsm oojv gqjx loyx
FROM_EMAIL=jamalsadaf3@gmail.com


