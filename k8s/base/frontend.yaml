apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: digital-library
spec:
  selector:
    app: frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: digital-library
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: sadafjamal3/digital-library-frontend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
              name: http
